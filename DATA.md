# Masroofi — Data & State Contract

هذه الوثيقة تحدد طريقة تمثيل البيانات وحساب الرصيد والتخزين المحلي.

## 1. مصدر الحقيقة

مصدر الحقيقة الوحيد للتطبيق هو **سجل العمليات**.

لا نخزن الرصيد الحالي كقيمة مستقلة قابلة للانحراف عن السجل.

الرصيد يحسب دائماً هكذا:

**الرصيد = مجموع مبالغ الدخل - مجموع مبالغ المصروفات**

## 2. نموذج العملية

نستخدم نموذجاً واحداً للدخل والمصروف لتقليل التعقيد.

شكل منطقي مقترح:

```ts
type TransactionType = 'income' | 'expense'

interface Transaction {
  id: string
  type: TransactionType
  name: string
  amount: number
  occurredAt: string
  note?: string
  icon?: string
  createdAt: string
  updatedAt: string
}
```

هذه صيغة مرجعية وليست التزاماً بشكل التنفيذ النهائي إذا ظهر سبب تقني أفضل، لكن المعنى يجب أن يبقى نفسه.

## 3. قواعد الحقول

### `id`

- فريد لكل عملية.
- ثابت بعد الإنشاء.

### `type`

- `income` للدخل.
- `expense` للمصروف.

### `name`

- مطلوب.
- يتم Trim للمسافات الزائدة.
- لا يقبل قيمة فارغة.

### `amount`

- رقم موجب فقط داخل البيانات.
- لا نخزن المصروف كرقم سالب.
- `type` هو الذي يحدد تأثيره على الرصيد.
- يمثل ديناراً عراقياً.
- في النسخة الأولى نتعامل مع IQD كأعداد صحيحة بدون كسور.

مثال:

مصروف 25,000 د.ع يخزن:

- `amount = 25000`
- `type = expense`

### `occurredAt`

- وقت حدوث العملية فعلياً.
- قيمته الافتراضية هي الوقت الحالي عند فتح/إنشاء العملية.
- المستخدم يستطيع تعديله إذا كان يسجل عملية قديمة.
- هو التاريخ المستخدم في ترتيب السجل والعرض.

### `note`

- اختيارية.
- إذا كانت فارغة لا نحتاج تخزين نص فارغ إلا إذا التنفيذ يتطلب ذلك.

### `icon`

- اختيارية.
- لا يتم توليدها تلقائياً من الاسم.
- يختارها المستخدم بنفسه.

### `createdAt`

- وقت إنشاء السجل داخل التطبيق.
- لا يعدله المستخدم.

### `updatedAt`

- آخر وقت تم فيه تعديل السجل.
- يتحدث عند كل تعديل.

## 4. الحسابات

### إجمالي الدخل

مجموع `amount` لكل عملية `type = income`.

### إجمالي المصروفات

مجموع `amount` لكل عملية `type = expense`.

### الرصيد الحالي

`totalIncome - totalExpenses`

### قاعدة مهمة

أي View يعرض الرصيد أو الإجماليات يجب أن يشتقها من نفس مجموعة العمليات الحالية، وليس من Cache مستقل غير موثوق.

## 5. الترتيب

السجل العام والدخل والمصروفات ترتب افتراضياً:

1. `occurredAt` من الأحدث إلى الأقدم.
2. عند تساوي الوقت يمكن استخدام `createdAt` كترتيب ثانوي ثابت.

## 6. CRUD

### Create

- إنشاء ID جديد.
- Validate الاسم والمبلغ.
- `occurredAt` = اختيار المستخدم أو الوقت الحالي.
- حفظ العملية.
- تحديث الواجهة فوراً.

### Update

- تعديل نفس `id`.
- لا يتم إنشاء عملية بديلة جديدة.
- تحديث `updatedAt`.
- إعادة حساب كل القيم المشتقة فوراً.

### Delete

- حذف العملية ذات `id` المحدد بعد تأكيد المستخدم.
- إعادة حساب القيم المشتقة فوراً.

## 7. IndexedDB

الهدف هو طبقة تخزين صغيرة وواضحة، وليست Architecture كبيرة.

اسم مقترح للقاعدة:

`masroofi`

Object Store أساسي:

`transactions`

Indexes المفيدة عند الحاجة:

- `type`
- `occurredAt`

نستخدم Database schema version واضحة حتى يمكن تنفيذ Migration مستقبلاً إذا تغير النموذج.

## 8. State داخل React

- IndexedDB هي مصدر التخزين الدائم.
- التطبيق يحتفظ بنسخة State حالية للعرض السريع.
- بعد أي عملية كتابة ناجحة، يجب أن تتطابق State المعروضة مع البيانات المخزنة.
- لا ننشئ أكثر من مصدر حقيقة مالي داخل التطبيق.

## 9. Backup

صيغة النسخة الاحتياطية المقترحة:

```json
{
  "format": "masroofi-backup",
  "version": 1,
  "exportedAt": "ISO_DATE",
  "transactions": []
}
```

### التصدير

- يحتوي جميع العمليات.
- لا يحتوي Secrets لأنه لا توجد Secrets أصلاً.
- اسم الملف يمكن أن يتضمن التاريخ.

### الاسترجاع

قبل الاسترجاع:

- فحص `format`.
- فحص `version`.
- فحص أن `transactions` Array.
- Validate كل Transaction.
- رفض الملف غير الصالح برسالة واضحة.

النسخة الأولى تفضّل **Replace بعد Confirmation** بدلاً من Merge تلقائي قد يكرر العمليات.

## 10. سلامة البيانات

يجب منع الحالات التالية:

- مبلغ صفر أو سالب داخل الحقل.
- اسم فارغ.
- Type غير معروف.
- تاريخ غير صالح.
- Backup مشوه يمسح البيانات الحالية.
- Update أو Delete لعملية غير موجودة بدون معالجة واضحة.

## 11. حدود الضمان المحلي

IndexedDB تحفظ البيانات عادة بين الجلسات، لكن لا يمكن ضمان بقائها إذا قام المستخدم بمسح بيانات الموقع أو المتصفح.

لهذا Backup/Restore جزء أساسي من تعريف Production للمشروع، وليس Cloud Sync.
