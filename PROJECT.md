# Masroofi — Project Source of Truth

> هذه الوثيقة هي المرجع الأساسي للمشروع. أي قرار جديد يغيّر سلوك التطبيق يجب أن ينعكس هنا أو في ملفات التوثيق المرتبطة قبل/مع تغييره في الكود.

## 1. الفكرة

**Masroofi (مصروفي)** تطبيق Web شخصي بسيط لإدارة الدخل والمصروفات والرصيد الحالي.

المشروع مصمم للاستخدام الشخصي فقط، ويعمل بدون حسابات مستخدمين وبدون Backend أو قاعدة بيانات خارجية.

## 2. المبدأ الأساسي

الرصيد ليس رقماً مستقلاً يتم تعديله يدوياً، وإنما نتيجة محسوبة دائماً من السجل:

**الرصيد الحالي = مجموع الدخل - مجموع المصروفات**

بالتالي أي إضافة أو تعديل أو حذف لعملية دخل أو مصروف يجب أن ينعكس فوراً على الرصيد وكل السجلات المرتبطة.

## 3. صفحات التطبيق

1. **الرئيسية**
   - عرض الرصيد الحالي بوضوح.
   - زر **إضافة رصيد**.
   - زر إضافة الرصيد هو نفسه إضافة دخل، ولا يوجد مفهوم مستقل باسم "إضافة عملية".
   - عرض آخر العمليات بشكل مختصر.

2. **المصروفات**
   - إضافة مصروف.
   - عرض سجل المصروفات.
   - تعديل المصروف.
   - حذف المصروف.

3. **الدخل**
   - إضافة دخل/رصيد.
   - عرض سجل الدخل.
   - تعديل الدخل.
   - حذف الدخل.

4. **السجل**
   - يجمع الدخل والمصروفات معاً.
   - مرتب زمنياً.
   - يعكس التعديلات والحذف فوراً.

## 4. بيانات كل عملية

كل عملية، سواء كانت دخلاً أو مصروفاً، تحتوي على:

- الاسم — مطلوب.
- المبلغ — مطلوب.
- النوع — دخل أو مصروف.
- التاريخ والوقت — تلقائياً تاريخ ووقت إنشاء العملية، مع إمكانية تعديله اختيارياً قبل الحفظ أو عند التعديل.
- ملاحظة — اختيارية.
- أيقونة/إيموجي — اختيارية، يختارها المستخدم بنفسه ولا يتم افتراضها تلقائياً.

## 5. العملة

- كل المبالغ بالدينار العراقي فقط.
- العرض يكون بصيغة بسيطة مثل: `250,000 د.ع`.
- لا يوجد اختيار عملة ولا نظام Multi-currency.

## 6. التخزين

- التخزين الأساسي محلي داخل المتصفح باستخدام **IndexedDB**.
- لا يوجد Backend.
- لا يوجد Login أو Authentication.
- لا يوجد Cloud Database.
- يجب أن تبقى البيانات محفوظة بين الجلسات ما لم يمسح المستخدم بيانات الموقع/المتصفح.
- لتقليل خطر فقدان البيانات، يتضمن الإصدار النهائي ميزة **تصدير نسخة احتياطية JSON** و **استرجاع نسخة JSON**.

## 7. التقنية المستهدفة

- React
- TypeScript
- Vite
- CSS منظم وخفيف
- IndexedDB
- GitHub Pages للاستضافة

الهدف هو إبقاء المشروع خفيفاً؛ لا تضاف مكتبات أو معماريات معقدة دون حاجة حقيقية.

## 8. اللغة والواجهة

- اللغة الأساسية: العربية.
- اتجاه الواجهة: RTL.
- تصميم Mobile-first ويعمل جيداً على الهاتف والكمبيوتر.
- واجهة بسيطة، نظيفة، هادئة، وغير مزدحمة.
- الأخضر للدخل/القيم الموجبة، والأحمر للمصروفات/القيم السالبة، مع الحفاظ على وضوح النص وعدم الاعتماد على اللون وحده.

## 9. التزامن الداخلي

مصدر الحقيقة الوحيد هو سجل العمليات المخزن محلياً.

عند إضافة/تعديل/حذف أي عملية يجب تحديث ما يظهر للمستخدم فوراً في:

- الرصيد الحالي.
- سجل الدخل.
- سجل المصروفات.
- السجل العام.
- الملخصات الرقمية ذات الصلة.

لا يتم الاحتفاظ بنسخ منفصلة غير متزامنة من الرصيد.

## 10. خارج نطاق النسخة الحالية

لا يتم تنفيذ هذه الأشياء إلا بقرار لاحق واضح:

- حسابات مستخدمين.
- Backend أو API خاص بالمشروع.
- مزامنة سحابية بين الأجهزة.
- ربط مصرفي أو مالي حقيقي.
- تعدد العملات.
- ميزانيات معقدة أو محاسبة احترافية.
- Features اجتماعية.

## 11. تعريف النجاح

المشروع يعتبر جاهزاً للإنتاج عندما يستطيع المستخدم من GitHub Pages أن:

1. يضيف دخلاً أو مصروفاً بسهولة.
2. يرى الرصيد الصحيح فوراً.
3. يعدل أو يحذف أي عملية ويظل كل شيء متزامناً.
4. يرجع لأي سجل سابق محفوظ محلياً.
5. يغلق الموقع ويفتحه لاحقاً وتبقى بياناته موجودة.
6. يصدر نسخة احتياطية ويسترجعها بشكل آمن.
7. يستخدم التطبيق بشكل مريح على الهاتف والكمبيوتر.
8. ينجح Build والفحوصات المتفق عليها بدون أخطاء حرجة.

## 12. قاعدة العمل

قبل تنفيذ أي Feature جديدة:

- اقرأ `PROJECT.md`.
- راجع `PLAN.md` و`CHECKLIST.md`.
- لا توسع Scope المشروع بدون قرار واضح.
- أي تغيير جوهري في المتطلبات يجب توثيقه.
