# Masroofi — Design Contract

## الاتجاه البصري

- واجهة عربية RTL نظيفة وحديثة.
- خلفية فاتحة جداً مع أسطح بيضاء.
- الأخضر هو اللون الأساسي للدخل والإجراءات الإيجابية.
- الأحمر للمصروفات والحذف فقط.
- زوايا دائرية هادئة، ظلال خفيفة، ومساحات مريحة.
- التصميم Mobile-first لكن يجب أن يبقى مريحاً على Desktop.

## الصفحات

1. الرئيسية: الرصيد الحالي، إضافة رصيد، ملخص الدخل والمصروفات، آخر العمليات.
2. المصروفات: إجمالي المصروفات، إضافة مصروف، سجل المصروفات، تعديل وحذف.
3. الدخل: إجمالي الدخل، إضافة دخل، سجل الدخل، تعديل وحذف.
4. السجل: الرصيد الحالي، فلترة الكل/الدخل/المصروفات، سجل زمني موحد، تعديل وحذف.

## إدخال التاريخ والوقت

- عند إنشاء أي عملية، التاريخ والوقت الحاليان هما القيمة الافتراضية.
- تعديل التاريخ اختياري.
- ممنوع الاعتماد على Date Picker قديم أو غير منسجم بصرياً مع التطبيق.
- يستخدم التطبيق Calendar مخصص حديث:
  - Dialog على الشاشات الواسعة.
  - Bottom Sheet لطيف على الهاتف.
  - تنقل واضح بين الأشهر.
  - اليوم الحالي مميز.
  - اليوم المختار مميز بوضوح.
  - زر سريع `اليوم`.
  - تعديل الوقت داخل نفس التجربة.
- يجب أن يكون الوصول للتاريخ واضحاً من خلال زر/حقل يعرض التاريخ الحالي مع كلمة `تعديل`.

## Emoji / Icon

- اختيار Emoji اختياري تماماً.
- لا يتم اختيار Emoji تلقائياً عند كتابة اسم العملية.
- يوجد خيار واضح `بدون`.
- إذا لم يختر المستخدم Emoji، تعرض الواجهة أيقونة نظام عامة فقط كجزء من UI ولا تُخزن كاختيار للمستخدم.

## الملاحظات

- الملاحظة اختيارية في الدخل والمصروف.
- تظهر في السجل عندما تكون موجودة فقط.

## العملة

- جميع المبالغ IQD وتعرض بصيغة `د.ع`.
- لا يوجد Currency selector ولا شرح زائد عن العملة في الواجهة.

## التزامن

أي إضافة أو تعديل أو حذف يجب أن يظهر فوراً في:
- الرصيد.
- ملخص الدخل والمصروفات.
- الصفحة الخاصة بالنوع.
- السجل العام.

## Navigation

Bottom Navigation ثابت بأربع وجهات:
- الرئيسية
- المصروفات
- الدخل
- السجل

`إضافة رصيد` في الرئيسية ينقل مباشرة إلى Flow إضافة دخل، ولا يوجد زر منفصل اسمه `إضافة عملية`.
