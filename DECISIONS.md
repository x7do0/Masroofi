# Masroofi — Technical Decisions

## 1. Frontend stack

React + TypeScript + Vite لتطبيق شخصي خفيف يعمل على GitHub Pages.

## 2. TypeScript version

تم تثبيت TypeScript 6.0.3 حالياً بدلاً من 7.x لأن `typescript-eslint` 8.65 يعلن رسمياً دعم TypeScript أقل من 6.1. الهدف تجنب تحذيرات وعدم توافق Tooling بدون فائدة للمشروع.

## 3. التخزين

IndexedDB هو مصدر التخزين المحلي. لا Backend ولا حسابات ولا API.

## 4. مصدر الحقيقة المالي

لا يتم تخزين الرصيد كرقم مستقل. الرصيد = مجموع الدخل - مجموع المصروفات.

## 5. نموذج البيانات

نستخدم Transaction واحد بنوع `income | expense` بدل جدولين منفصلين، حتى التزامن والحسابات والسجل العام تبقى أبسط وأوثق.

## 6. التاريخ

التاريخ والوقت الحاليان افتراضيان عند الإضافة، مع إمكانية تعديلهما. واجهة تعديل التاريخ Calendar مخصصة وحديثة وليست native date input قديم.

## 7. الأيقونة

Emoji اختياري ولا يتم استنتاجه تلقائياً من الاسم.

## 8. التنقل

لا نحتاج Router حالياً. التطبيق أربع Views داخل Shell واحد، وهذا يتجنب تعقيدات Refresh/404 على GitHub Pages ويحافظ على البساطة.

## 9. الأيقونات

Lucide React للأيقونات النظامية فقط. Emoji المستخدم منفصل واختياري.

## 10. الجودة البصرية

تصميم Mobile-first، RTL، أسطح فاتحة، أخضر للدخل والإجراءات الأساسية، وأحمر للمصروف/الحذف. لا نكثر مكتبات UI ولا نعتمد Browser-default controls في التجارب البارزة مثل اختيار التاريخ.
