# Masroofi — UI / UX Design Contract

هذه الوثيقة تحدد شكل وسلوك الواجهة حتى يبقى التطبيق بسيطاً ومتناسقاً خلال التطوير.

## المبادئ

- عربي بالكامل وRTL.
- Mobile-first.
- واجهة هادئة ونظيفة بدون ازدحام.
- أقل عدد ممكن من الأزرار والخيارات.
- كل إجراء مالي واضح ومباشر.
- لا نعرض تفاصيل تقنية للمستخدم لا يحتاجها.
- لا نبالغ في شرح أن العملة IQD؛ نعرضها ببساطة كـ `د.ع`.

## الهوية البصرية

- خلفية فاتحة ومحايدة.
- أخضر هادئ للدخل والقيم الموجبة والإجراءات الأساسية.
- أحمر هادئ للمصروفات والقيم السالبة والحذف.
- بطاقات بحواف دائرية ومسافات مريحة.
- الظلال خفيفة جداً عند الحاجة.
- الخط عربي واضح ومقروء.

## التنقل الرئيسي

Bottom Navigation ثابت أو شبه ثابت يحتوي أربع وجهات فقط:

1. الرئيسية
2. المصروفات
3. الدخل
4. السجل

لا نضيف تبويبات أساسية أخرى إلا بقرار جديد واضح.

## الرئيسية

### العناصر الأساسية

- اسم التطبيق/عنوان بسيط.
- بطاقة كبيرة تعرض:
  - `الرصيد الحالي`
  - المبلغ الحالي.
- زر أساسي: `إضافة رصيد`.
- قسم `آخر العمليات` يعرض آخر عدد صغير من العمليات.
- رابط/زر بسيط للانتقال للسجل الكامل إذا احتجناه.

### قاعدة مهمة

لا يوجد زر مستقل باسم `إضافة عملية`.

`إضافة رصيد` = إضافة دخل، بنفس البيانات ونفس قواعد الحفظ.

## صفحة المصروفات

### أعلى الصفحة

- عنوان `المصروفات`.
- يمكن عرض إجمالي مصروفات بسيط إذا خدم الوضوح، بدون Dashboard معقد.

### إضافة مصروف

الحقول:

1. `اسم المصروف` — مطلوب.
2. `المبلغ` — مطلوب.
3. `التاريخ والوقت` — القيمة الافتراضية الآن، ويمكن للمستخدم تعديلها اختيارياً.
4. `ملاحظة` — اختيارية.
5. `أيقونة / إيموجي` — اختيارية ويختارها المستخدم يدوياً.

الزر الأساسي: `إضافة مصروف`.

### السجل

كل صف يعرض بالحد الأدنى:

- الأيقونة إن وجدت، وإلا Placeholder بسيط أو بدون أيقونة حسب التصميم النهائي.
- الاسم.
- التاريخ/الوقت.
- المبلغ بصيغة سالبة أو بأسلوب بصري واضح للمصروف.
- إمكانية فتح التعديل.
- إمكانية الحذف.

## صفحة الدخل

نفس فلسفة المصروفات، لكن:

- العنوان `الدخل`.
- الزر `إضافة دخل`.
- المبلغ يظهر كقيمة موجبة.
- اللون الأساسي أخضر.

الحقول نفسها:

- الاسم.
- المبلغ.
- التاريخ والوقت التلقائيان مع تعديل اختياري.
- ملاحظة اختيارية.
- أيقونة/إيموجي اختيارية.

## صفحة السجل

- تجمع كل العمليات.
- الأحدث أولاً بشكل افتراضي.
- كل عملية يجب أن توضح هل هي دخل أم مصروف.
- اللون يساعد، لكنه ليس العلامة الوحيدة.
- يمكن استخدام Label صغير `دخل` أو `مصروف`.
- التعديل والحذف يجب أن يكونا متاحين من Flow واضح، مباشرة أو عبر تفاصيل العملية.

## نموذج الإضافة والتعديل

نفضّل استخدام نفس Component/Flow للإضافة والتعديل لتجنب اختلاف السلوك.

### عند الإضافة

- التاريخ والوقت يبدآن تلقائياً بالوقت الحالي.
- المستخدم لا يحتاج لمس حقل التاريخ إذا العملية تحدث الآن.
- اختيار الإيموجي لا يفتح تلقائياً ولا يفرض قيمة.
- الملاحظة فارغة افتراضياً.

### عند التعديل

- تظهر القيم الحالية كلها.
- حفظ التعديل يحدّث العملية نفسها، وليس إنشاء نسخة جديدة.
- الرصيد والسجلات تتحدث فوراً بعد الحفظ.

## الحذف

- الحذف يحتاج Confirmation بسيط وواضح.
- لا نستخدم صياغة مخيفة أو معقدة.
- بعد الحذف تتحدث كل الحسابات والسجلات فوراً.

## المبالغ

أمثلة عرض:

- دخل: `+250,000 د.ع`
- مصروف: `-15,000 د.ع`
- رصيد: `235,000 د.ع`

لا يوجد Dropdown للعملة.

## التاريخ

- العرض للمستخدم يكون عربي ومقروء.
- التخزين يكون بقيمة زمنية ثابتة مناسبة برمجياً.
- التاريخ الذي يختاره المستخدم يمثل وقت حدوث العملية، وليس وقت إنشاء السجل التقني.

## الملاحظات

- اختيارية بالكامل.
- لا تأخذ مساحة كبيرة إذا كانت فارغة.
- عند وجودها يمكن إظهارها في تفاصيل العملية أو كسطر ثانوي مختصر.

## الأيقونات / الإيموجي

- اختيارية.
- لا يتم تعيين Emoji بناءً على اسم العملية تلقائياً.
- المستخدم هو من يختار.
- عدم الاختيار لا يمنع الحفظ.
- قائمة الاختيار يجب أن تكون صغيرة ومناسبة، لا Emoji picker ضخم ومعقد في النسخة الأولى.

## Empty States

أمثلة:

- لا توجد مصروفات بعد.
- لا يوجد دخل مسجل بعد.
- لا توجد عمليات في السجل بعد.

يجب أن تقود الرسالة للإجراء المناسب بدون شروحات طويلة.

## Responsive

### الهاتف

هو التجربة الأساسية.

- أزرار سهلة للمس.
- Bottom Navigation واضحة.
- البطاقات بعرض مناسب.
- النماذج عمودية.

### الكمبيوتر

- لا يتم تمديد المحتوى على كامل الشاشة بشكل مزعج.
- نستخدم عرض محتوى مركزي مريح.
- نفس الوظائف ونفس التسلسل المنطقي.

## ما لا نريده في التصميم حالياً

- Charts كثيرة.
- Dashboard احترافي معقد.
- Categories إجبارية.
- ميزانيات شهرية.
- أهداف ادخار.
- مؤثرات وحركات كثيرة.
- شاشات Onboarding طويلة.
- إعدادات كثيرة.
