# Masroofi — Data Contract

## Transaction

```ts
type TransactionType = 'income' | 'expense' | 'debt_given' | 'debt_repayment'

type Transaction = {
  id: string
  title: string
  amount: number
  occurredAt: string
  note: string | null
  emoji: string | null
  createdAt: string
  updatedAt: string
} & (
  | { type: 'income' | 'expense' | 'debt_given'; debtId?: never }
  | { type: 'debt_repayment'; debtId: string }
)
```

- `debt_given`: إعطاء دين؛ `title` هو اسم الشخص.
- `debt_repayment`: استرجاع مبلغ؛ `debtId` يشير إلى ID عملية `debt_given`.
- يحتفظ التسديد باسم الشخص في `title`، والعرض يقرأ الاسم الحالي من أصل الدين حتى ينعكس تعديل الاسم على السجل.
- يمكن أن يملك الشخص عدة ديون مستقلة. التسديد مرتبط بدين محدد، وليس بالاسم.

## مصدر الحقيقة والحسابات

العمليات هي مصدر الحقيقة الوحيد. لا يُخزن الرصيد أو حالة الدين بصورة مستقلة. الحسابات المركزية في `src/services/ledger.ts`:

- الدخل = مجموع `income` فقط.
- المصروفات والاستهلاك = مجموع `expense` فقط.
- الرصيد النقدي = الدخل − المصروفات − الديون المعطاة + التسديدات.
- المستحق لك = الديون المعطاة − التسديدات.
- المتبقي من كل دين = مبلغه الأصلي − مجموع دفعاته المرتبطة.
- الحالة: `unpaid` إذا لم يسدد شيء، `partial` إذا بقي جزء، `paid` إذا كان المتبقي صفراً.

`getTotals` و`getDebts` تعيدان هذه القيم للواجهات. يستخدم الجمع `bigint` داخلياً ثم يحول إلى أرقام آمنة؛ البيانات المخزنة والنسخ الاحتياطية تبقى أرقام JSON عادية.

## التخزين والتوافق مع البيانات القديمة

- IndexedDB database: `masroofi-db`.
- Version: **2**.
- Store: `transactions`، المفتاح `id`، الفهارس `occurredAt` و`type`.
- الترقية من الإصدار 1 إلى 2 لا تحذف أو تعيد كتابة أي متجر أو سجل: نفس المتجر والفهارس تقبل الأنواع الجديدة والحقل الإضافي `debtId`، وعمليات الدخل والمصاريف القديمة تبقى كما هي.
- رفع إصدار قاعدة البيانات يحمي العلاقات الجديدة من نوافذ التطبيق القديمة: الكود القديم يطلب الإصدار 1 فلا يستطيع فتح القاعدة بعد ترقيتها وكتابة بيانات لا تراعي تسديدات الدين.
- عند حجز الترقية بواسطة نافذة قديمة تظهر رسالة تطلب إغلاقها وإعادة فتح التطبيق، بدلاً من انتظار صامت. أي اتصال يكتمل بعد هذا الإلغاء يغلق دون تعديل البيانات.
- إصدار تنسيق النسخة الاحتياطية منفصل عن إصدار مخطط IndexedDB.
- البيانات محلية في المتصفح، ولا توجد مزامنة سحابية أو تغيير لطريقة التخزين.

## التحقق والتعديل والحذف

- المبلغ عدد صحيح موجب ضمن `Number.MAX_SAFE_INTEGER`؛ لا تقريب صامت للكسور.
- يتم التحقق من التواريخ والحقول وIDs المكررة والروابط ومجاميع المبالغ.
- لا يجوز ربط التسديد بعملية غير موجودة أو بعملية غير `debt_given`.
- لا يجوز أن يزيد مجموع التسديدات عن أصل الدين؛ تعديل دفعة أو تقليل أصل الدين يخضع لنفس القاعدة.
- نوع العملية وربط `debtId` ثابتان عند التعديل والدمج، لمنع نقل دفعات إلى ديون أخرى أو تغيير طبيعة العملية.
- حذف الدين يحذف كل دفعاته ضمن معاملة واحدة. حذف دفعة وحدها يعيد المبلغ المتبقي ويصحح الرصيد آلياً.
- القراءة والتحقق والكتابة تتم داخل معاملة IndexedDB واحدة من نوع `readwrite`. المتصفح يسلسلها بين النوافذ، لذلك لا يمكن لدفعتين متزامنتين تجاوز المتبقي.
- لا يُعلن النجاح إلا بعد حدث `complete`؛ أي فشل يلغي المعاملة كاملة.

## التاريخ والتزامن

`occurredAt` يمثل التاريخ والوقت المختار. يدعم السجل قيم التاريخ المحلي القديمة وقيم ISO، ويرتبها حسب الوقت الفعلي المحلل بـ`Date`، وليس المقارنة النصية. لا يعاد تفسير أو تغيير النصوص المخزنة أثناء الترقية.

بعد اكتمال الكتابة تعيد الواجهة قراءة الحالة المحفوظة. تستخدم `BroadcastChannel` لتحديث النوافذ الأخرى، مع إعادة تحميل عند التركيز والعودة إلى الصفحة كمسار بديل. رقم تسلسل يمنع نتيجة تحميل قديمة من استبدال حالة أحدث.

## النسخ الاحتياطي والاسترجاع

التصدير الجديد:

```ts
{
  format: 'masroofi-backup'
  version: 2
  exportedAt: string
  transactions: Transaction[]
}
```

- الاستيراد يقبل النسخة **1** القديمة للدخل والمصاريف والنسخة **2** التي تدعم الأنواع الأربعة.
- يتم التحقق من كل الحقول ومن ترابط الدين وتسديداته ومن حدود المبالغ قبل تغيير البيانات.
- **الدمج:** يقرأ أحدث بيانات المتصفح داخل المعاملة، ويختار العملية ذات `updatedAt` الأحدث لكل ID؛ عند تعادل الوقت يحتفظ بالموجود. ثم يفحص السجل الناتج كله قبل كتابة أي تعديل.
- قد تكون نسختان صحيحتين منفردتين لكن ينتج عن دمجهما تسديد زائد. عند ذلك يُرفض الدمج كله برسالة واضحة وتبقى البيانات الحالية دون تغيير.
- **الاستبدال:** يستبدل السجل كاملاً بالنسخة بعد اختيار المستخدم وتأكيده في الواجهة. التحقق والكتابة ذريان، وأي فشل يبقي السجل السابق.
- النسخ القديمة لا تحتوي الديون الجديدة؛ استبدال السجل بها يسترجع حالتها القديمة، بينما الدمج يحافظ على الديون الموجودة حالياً.
